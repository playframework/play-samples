[versions]
play = "3.1.0-M10-08097189-SNAPSHOT"
junit = "4.13.2"

[libraries]
junit = { group = "junit", name = "junit", version.ref = "junit" }

[plugins]
play = { id = "org.playframework.play", version.ref = "play" }
